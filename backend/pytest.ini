[pytest]
testpaths = tests
timeout = 1800
markers =
    ml: usa modelos reales (Whisper/wav2vec) ya descargados; ejecutar con RUN_ML_TESTS=1
    slow: pruebas lentas (llamada larga, entrenamiento)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
