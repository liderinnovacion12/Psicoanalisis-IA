# --- Núcleo web / datos -------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sqlalchemy>=2.0.30
psycopg[binary]>=3.2
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0
pyjwt>=2.8
bcrypt>=4.1
cryptography>=42
email-validator>=2.1

# --- Cola de tareas -----------------------------------------------------
celery>=5.4
redis>=5.0

# --- Almacenamiento -----------------------------------------------------
boto3>=1.34            # S3 / MinIO (opcional en runtime)

# --- Audio / señal ------------------------------------------------------
numpy>=1.26
scipy>=1.11
soundfile>=0.12
librosa>=0.10
imageio-ffmpeg>=0.5    # binario de FFmpeg de respaldo si no está en el PATH

# --- Reportes / exportación --------------------------------------------
reportlab>=4.1
matplotlib>=3.8
openpyxl>=3.1

# --- Utilidades ---------------------------------------------------------
psutil>=5.9
scikit-learn>=1.4
joblib>=1.3
